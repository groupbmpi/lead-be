const Instance = require('../models/instance');
const Participant = require('../models/participant');
const InstanceParticipant = require('../models/instanceparticipant');

// pendaftaran instansi peserta (transaction)
const registerParticipant = async (req, res) => {
}

// penambahan peserta/representatif instansi peserta (transaction)
const addParticipant = async (req, res) => {
    
}